# Copy to keystore.properties (do not commit) and fill in:
# - storeFile: path to your JKS/PKCS12 keystore (recommended: release.jks in this folder)
# - storePassword / keyAlias / keyPassword: signing credentials
#
# For CI, set GitHub Secrets (see HANDOFF.md):
# - ANDROID_KEYSTORE_BASE64
# - ANDROID_KEYSTORE_PASSWORD
# - ANDROID_KEY_ALIAS
# - ANDROID_KEY_PASSWORD

storeFile=release.jks
storePassword=CHANGE_ME
keyAlias=release
keyPassword=CHANGE_ME
